# Company MacBook: macOS, zsh (plan_a.md 5.1).
# Most of the implementation and every paid LLM call.
# Not set up yet: values below are placeholders until the first MacBook session
# runs `utrack doctor` and Decisions C, I, K, L are resolved (configs/decisions.md).
machine: macbook
role: implementation-and-paid-calls
network:
  github: unknown # checked by `utrack doctor` at the first session (Decision L)
  huggingface: unknown # checked by `utrack doctor` at the first session (Decision L)
llm_providers: [] # filled in once Decision C is resolved
cost_cap_usd: null # filled in once Decision I is resolved
cache_dir: null # null = repo-relative default
hf_home: null # null = repo-relative default
